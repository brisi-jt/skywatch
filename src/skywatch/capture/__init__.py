"""Capture layer: everything between the SDR dongle and the pipeline.

``CaptureSource`` implementations produce clip files in the recordings
directory; the watcher turns those files into ``recordings`` rows.
"""
