# Copy to the repository root as .env — never commit the real file.
GEMINI_API_KEY=
GROQ_API_KEY=
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
